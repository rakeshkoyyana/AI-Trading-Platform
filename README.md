# SMC Trading Agent — AI-Powered Smart Money Concepts Trading Bot

An automated trading agent that ports an existing Pine Script "triple confirmation" SMC (Smart Money Concepts) strategy to Python, layers a machine-learning win-probability filter and live news sentiment on top of it, executes trades automatically on a market-hours schedule, and reports everything on a live dashboard.

> ⚠️ **Read before starting**
> - This is an engineering project, not investment advice, and nothing here should be treated as advice from a financial advisor.
> - **Robinhood has no official trading API.** The unofficial `robin_stocks` library works today but violates Robinhood's Terms of Service and can trigger account restrictions or break silently on any app update. This project builds execution against **Alpaca** (free, official REST API, real paper trading) first, behind a broker-agnostic interface. A Robinhood adapter is included as an optional, isolated, opt-in module — use it at your own risk.
> - Paper-trade for at least 2–4 weeks before risking real money.
> - Every tool used has a free tier (see below); the plan notes the real limits of each so nothing surprises you mid-build.

## Status

Track progress via the [GitHub Project board](#setting-this-up-on-github) and the milestones/issues generated by `scripts/create_github_issues.sh`. See [`TASKS.md`](TASKS.md) for the full checklist and [`docs/PROJECT_PLAN.md`](docs/PROJECT_PLAN.md) for the detailed step-by-step plan.

## Architecture

```
Market Data (Alpaca/yfinance) ──┐
                                  ├──> Feature Engineering (SMC logic ported from Pine Script) ──> ML Model (win-probability filter) ──┐
News Feed (Finnhub/NewsAPI) ────┤                                                                                                     │
                                  └──> Sentiment Scoring (FinBERT, local)  ──────────────────────────────────────────────────────────┤
                                                                                                                                        ↓
                                                                                                                          Decision Engine (triple confirmation + ML + sentiment + risk rules)
                                                                                                                                        ↓
                                                                                                                          Execution Layer (Alpaca first, Robinhood optional)
                                                                                                                                        ↓
                                                                                                                          Trade Logger (SQLite / Supabase)
                                                                                                                                        ↓
                                                                                                          Scheduler (8:30am–3:00pm CST) ⇄ Dashboard (Streamlit)
```

## Tech stack (all free-tier)

| Component | Tool | Cost | Notes |
|---|---|---|---|
| Market data | Alpaca Market Data API | Free (IEX feed) | Same account as execution |
| Market data (fallback) | `yfinance` | Free | No account needed, delayed data |
| Execution (primary) | Alpaca Trading API | Free, paper trading | Build/test here |
| Execution (optional, risky) | `robin_stocks` | Free, unofficial | Uses your real Robinhood login |
| News feed | Finnhub | Free: 60 calls/min | Primary |
| News feed (backup) | NewsAPI.org | Free: 100 req/day | ~15min delayed |
| Sentiment | FinBERT (`ProsusAI/finbert`) | Free, local | Runs via `transformers` |
| ML | scikit-learn, XGBoost | Free | |
| Scheduler | APScheduler | Free | |
| Database | SQLite | Free, local | |
| Database (optional, cloud) | Supabase | Free: 500MB | |
| Dashboard | Streamlit + Plotly | Free | Deploys free on Streamlit Community Cloud |
| Alerts | Discord webhook / Telegram bot | Free | |
| Version control | GitHub | Free | Private repo |

## Repo layout

```
smc-trading-agent/
├── data/                # raw + processed data, gitignored
├── src/
│   ├── data_ingestion/
│   ├── smc_logic/
│   ├── sentiment/
│   ├── ml/
│   ├── decision_engine/
│   ├── execution/
│   ├── scheduler/
│   └── dashboard/
├── tests/
├── notebooks/
├── models/
├── docs/
│   ├── PROJECT_PLAN.md
│   └── smc_logic_spec.md   # created in Phase 2
├── .env                  # never commit
├── .gitignore
├── requirements.txt
├── TASKS.md
└── README.md
```

## Timeline estimate

| Scenario | Estimate |
|---|---|
| Full-time (6–8 hrs/day) | ~7–8 weeks (35–40 working days) |
| Part-time (2–3 hrs/day) | ~12–16 weeks (3–4 months) |
| Plus paper-trading validation | add 2–4 weeks before considering live |

The biggest time sink is **Phase 2** (porting/validating SMC logic against TradingView) — budget generously there.

## Quick start

```bash
git clone https://github.com/<your-username>/smc-trading-agent.git
cd smc-trading-agent
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env   # fill in your API keys
python src/smoke_test.py
```

## Setting this up on GitHub

1. Create a **private** repo named `smc-trading-agent` on github.com.
2. Push this scaffold:
   ```bash
   git init && git branch -M main
   git remote add origin https://github.com/<your-username>/smc-trading-agent.git
   git add .
   git commit -m "Initial project scaffold + plan"
   git push -u origin main
   ```
3. Turn on a **GitHub Project** (Projects tab → New project → Board template) with columns `Backlog / To do / In progress / In review / Done`.
4. Run `scripts/create_github_issues.sh` (see comments inside) to auto-create one **milestone per phase** and one **issue per task** from `TASKS.md`, each labeled by phase — then add them all to the Project board in one click from the Issues list.
5. Work phase by phase, checking off tasks and closing issues as you go. `docs/PROJECT_PLAN.md` is your reference for *how* to do each task; `TASKS.md`/Issues are your tracker for *whether* it's done.

## License / disclaimer

Personal project scaffold. Not affiliated with Alpaca, Robinhood, Finnhub, or Anthropic. No warranty; use at your own risk, especially anything that touches live order execution.
